# تطبيق ويب لمراقبة جودة الهواء 🌡️

تطبيق ويب متكامل لعرض بيانات الحرارة والرطوبة من ESP32 + DHT11 في الوقت الفعلي باستخدام Node.js, Express, WebSocket, HTML, CSS, و JavaScript.

![Node.js](https://img.shields.io/badge/Node.js-43853D?style=flat&logo=node.js&logoColor=white)
![Express](https://img.shields.io/badge/Express-000000?style=flat&logo=express&logoColor=white)
![WebSocket](https://img.shields.io/badge/WebSocket-010101?style=flat&logo=socket.io&logoColor=white)
![ESP32](https://img.shields.io/badge/ESP32-000000?style=flat&logo=espressif&logoColor=white)

## ✨ الميزات

### 📊 عرض البيانات
- ✅ عرض فوري للحرارة والرطوبة ومؤشر الحرارة
- ✅ تحديث تلقائي في الوقت الفعلي عبر WebSocket
- ✅ واجهة عصرية وجميلة
- ✅ متجاوبة مع جميع الأجهزة

### 📈 الإحصائيات
- ✅ أعلى وأدنى قيمة للحرارة والرطوبة
- ✅ المتوسطات الحسابية
- ✅ إجمالي عدد القراءات
- ✅ وقت آخر تحديث

### 📉 الرسم البياني
- ✅ رسم بياني تفاعلي للبيانات التاريخية
- ✅ عرض آخر 10/20/50/100 قراءة
- ✅ تحديث تلقائي مع القراءات الجديدة

### 💾 التصدير
- ✅ تصدير البيانات بصيغة CSV
- ✅ تصدير البيانات بصيغة JSON
- ✅ حفظ الإحصائيات مع البيانات

### 🎨 التصميم
- ✅ واجهة عصرية بألوان متدرجة
- ✅ أيقونات تعبيرية (Emojis)
- ✅ رسوم متحركة سلسة
- ✅ دعم الوضع الداكن (قريباً)

## 📋 المتطلبات

### على جهاز الكمبيوتر (Server)
- Node.js (v14 أو أحدث)
- npm (يأتي مع Node.js)

### على ESP32
- ESP32 Development Board
- DHT11 Sensor
- كابل USB
- Arduino IDE

## 🚀 التثبيت والتشغيل

### 1️⃣ تثبيت Node.js

#### Windows
1. حمّل من: https://nodejs.org/
2. اختر النسخة LTS
3. ثبّت البرنامج (Next → Next → Install)
4. تحقق من التثبيت:
```bash
node --version
npm --version
```

#### macOS
```bash
# باستخدام Homebrew
brew install node
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nodejs npm

# Fedora
sudo dnf install nodejs npm
```

### 2️⃣ تثبيت تطبيق الويب

```bash
# انتقل إلى مجلد webapp
cd webapp

# تثبيت المكتبات المطلوبة
npm install
```

### 3️⃣ تشغيل السيرفر

```bash
# تشغيل عادي
npm start

# أو تشغيل مع إعادة التحميل التلقائي (للتطوير)
npm run dev
```

ستظهر رسالة:
```
╔════════════════════════════════════════════╗
║   🌡️  Air Quality Monitoring Server      ║
╚════════════════════════════════════════════╝

🚀 Server running on: http://localhost:3000
📡 API endpoint: http://localhost:3000/api/data
```

### 4️⃣ فتح التطبيق

افتح المتصفح واذهب إلى:
```
http://localhost:3000
```

### 5️⃣ إعداد ESP32

1. افتح `esp32_client.ino` في Arduino IDE

2. عدّل إعدادات WiFi:
```cpp
const char* ssid = "اسم_شبكتك";
const char* password = "كلمة_المرور";
```

3. عدّل عنوان Server:
```cpp
// اكتشف IP جهازك:
// Windows: ipconfig
// Mac/Linux: ifconfig

const char* serverUrl = "http://192.168.1.100:3000/api/data";
// غيّر 192.168.1.100 إلى IP جهازك
```

4. ثبّت المكتبة المطلوبة:
   - **ArduinoJson** (من Library Manager)

5. ارفع الكود على ESP32

6. افتح Serial Monitor (115200 baud)

7. ستبدأ البيانات بالظهور على التطبيق! 🎉

## 📁 هيكل المشروع

```
webapp/
├── server.js              # Node.js server
├── package.json           # معلومات المشروع والمكتبات
├── esp32_client.ino       # كود ESP32 للإرسال
├── README.md              # هذا الملف
│
└── public/                # الملفات العامة
    ├── index.html         # الصفحة الرئيسية
    ├── styles.css         # التصميم
    └── app.js             # JavaScript للعميل
```

## 🔌 API Endpoints

### GET /
الصفحة الرئيسية للتطبيق

### GET /api/current
الحصول على القراءات الحالية

**استجابة:**
```json
{
  "success": true,
  "data": {
    "temperature": 24.5,
    "humidity": 55.0,
    "heatIndex": 24.3,
    "timestamp": 1234567890,
    "status": "active"
  },
  "stats": {
    "maxTemp": 26.0,
    "minTemp": 22.0,
    ...
  }
}
```

### GET /api/history?limit=50
الحصول على البيانات التاريخية

### GET /api/stats
الحصول على الإحصائيات فقط

### POST /api/data
إرسال قراءة جديدة (من ESP32)

**Body:**
```json
{
  "temperature": 24.5,
  "humidity": 55.0,
  "heatIndex": 24.3
}
```

### GET /api/export/csv
تصدير البيانات كـ CSV

### GET /api/export/json
تصدير البيانات كـ JSON

### POST /api/clear
مسح جميع البيانات

## 🌐 WebSocket

التطبيق يستخدم WebSocket للتحديث الفوري:

```javascript
// الاتصال
ws://localhost:3000

// رسائل الاستقبال
{
  "type": "initial",     // البيانات الأولية
  "data": {...},
  "stats": {...}
}

{
  "type": "update",      // تحديث جديد
  "data": {...},
  "stats": {...}
}
```

## 🛠️ التخصيص

### تغيير المنفذ (Port)

في `server.js`:
```javascript
const PORT = process.env.PORT || 3000;
```

أو عند التشغيل:
```bash
PORT=8080 npm start
```

### تغيير معدل الإرسال

في `esp32_client.ino`:
```cpp
const unsigned long sendInterval = 2000;  // بالميلي ثانية
```

### تغيير عدد القراءات المحفوظة

في `server.js`:
```javascript
const MAX_HISTORY = 100;  // عدد القراءات
```

### تغيير الألوان

في `public/styles.css`:
```css
:root {
    --primary-color: #667eea;
    --secondary-color: #764ba2;
    ...
}
```

## 📊 لقطات الشاشة

### الصفحة الرئيسية
عرض القراءات الحالية مع بطاقات ملونة وجميلة

### الإحصائيات
عرض شامل لجميع الإحصائيات والقراءات التاريخية

### الرسم البياني
رسم بياني تفاعلي يعرض تطور الحرارة والرطوبة

## 🐛 استكشاف الأخطاء

### المشكلة: لا يعمل npm install

**الحل:**
```bash
# مسح cache
npm cache clean --force

# إعادة المحاولة
npm install
```

### المشكلة: ESP32 لا يرسل البيانات

**الحلول:**
1. تحقق من اتصال WiFi في Serial Monitor
2. تأكد من صحة IP address للسيرفر
3. تأكد من أن السيرفر يعمل
4. تأكد من أن الجهازين على نفس الشبكة
5. عطّل Firewall مؤقتاً للاختبار

### المشكلة: الصفحة تظهر "غير متصل"

**الحلول:**
1. تحقق من أن السيرفر يعمل
2. افتح Console في المتصفح (F12) وتحقق من الأخطاء
3. أعد تحميل الصفحة (Ctrl+F5)

### المشكلة: CORS Error

**الحل:**
مكتبة CORS مضمنة في المشروع، لكن إذا واجهت مشاكل:
```javascript
// في server.js
app.use(cors({
  origin: '*',  // للتطوير فقط!
  methods: ['GET', 'POST']
}));
```

## 🚀 التطويرات المستقبلية

- [ ] دعم عدة أجهزة ESP32
- [ ] حفظ البيانات في قاعدة بيانات
- [ ] مصادقة المستخدمين
- [ ] إشعارات عند تجاوز الحدود
- [ ] تطبيق موبايل
- [ ] دعم MQTT
- [ ] الوضع الداكن
- [ ] تقارير يومية/أسبوعية
- [ ] التكامل مع Google Sheets
- [ ] API توثيق (Swagger)

## 📚 المكتبات المستخدمة

### Server (Node.js)
- **express** - إطار عمل الويب
- **cors** - التعامل مع CORS
- **body-parser** - معالجة البيانات
- **ws** - WebSocket server

### Client (Browser)
- **Chart.js** - الرسوم البيانية
- **Vanilla JavaScript** - بدون مكتبات إضافية

### ESP32
- **WiFi.h** - الاتصال بالإنترنت
- **HTTPClient.h** - إرسال HTTP requests
- **DHT.h** - قراءة DHT11
- **ArduinoJson.h** - معالجة JSON

## 🤝 المساهمة

نرحب بمساهماتكم!

1. Fork المشروع
2. أنشئ branch جديد (`git checkout -b feature/amazing`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push إلى Branch (`git push origin feature/amazing`)
5. افتح Pull Request

## 📄 الترخيص

MIT License - استخدم المشروع بحرية!

## 💬 الدعم

إذا واجهت مشاكل أو لديك أسئلة:
- افتح Issue في GitHub
- راسلنا عبر البريد الإلكتروني
- انضم إلى مجموعة Telegram

## 🌟 شكر خاص

- مجتمع Arduino
- مجتمع ESP32
- مطوري المكتبات المستخدمة
- جميع المساهمين

---

**صُنع بـ ❤️ للمطورين العرب | 2025**

🌡️ **استمتع بمراقبة جودة الهواء!** 💧

