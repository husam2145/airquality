# دليل البدء السريع ⚡

## خطوات سريعة لتشغيل التطبيق في 10 دقائق!

### 1️⃣ تثبيت Node.js (دقيقتان)

**Windows:**
- حمّل من https://nodejs.org/
- ثبّت البرنامج (Next → Next → Install)

**Mac:**
```bash
brew install node
```

**Linux:**
```bash
sudo apt install nodejs npm
```

**التحقق:**
```bash
node --version
npm --version
```

---

### 2️⃣ تثبيت التطبيق (دقيقة)

```bash
cd webapp
npm install
```

انتظر حتى تنتهي عملية التثبيت...

---

### 3️⃣ تشغيل السيرفر (10 ثواني)

```bash
npm start
```

يجب أن ترى:
```
🚀 Server running on: http://localhost:3000
```

---

### 4️⃣ اختبار التطبيق (10 ثواني)

افتح المتصفح:
```
http://localhost:3000
```

ستظهر الواجهة (بدون بيانات حتى الآن)

---

### 5️⃣ إعداد ESP32 (5 دقائق)

1. افتح `esp32_client.ino`

2. **عدّل إعدادات WiFi:**
```cpp
const char* ssid = "اسم_شبكتك";           // ضع اسم شبكتك
const char* password = "كلمة_المرور";     // ضع كلمة المرور
```

3. **اكتشف IP جهازك:**

**Windows (CMD أو PowerShell):**
```bash
ipconfig
```
ابحث عن: `IPv4 Address` مثل `192.168.1.100`

**Mac/Linux:**
```bash
ifconfig
# أو
ip addr
```
ابحث عن: `inet` مثل `192.168.1.100`

4. **عدّل عنوان السيرفر:**
```cpp
const char* serverUrl = "http://192.168.1.100:3000/api/data";
//                              ^^^^^^^^^^^^
//                              ضع IP جهازك هنا
```

5. **ثبّت المكتبة:**
   - Arduino IDE → Sketch → Include Library → Manage Libraries
   - ابحث عن: `ArduinoJson`
   - اضغط Install

6. **ارفع الكود:**
   - Tools → Board → ESP32 Dev Module
   - Tools → Port → (اختر المنفذ)
   - Upload ⬆️

---

### 6️⃣ مشاهدة النتائج (الآن!)

1. افتح Serial Monitor (115200 baud)
2. ارجع للمتصفح
3. **شاهد البيانات تظهر مباشرة!** 🎉

---

## ✅ قائمة التحقق

- [ ] Node.js مثبت
- [ ] `npm install` نجح
- [ ] السيرفر يعمل على port 3000
- [ ] الصفحة تفتح في المتصفح
- [ ] ESP32 متصل بـ WiFi
- [ ] ESP32 يرسل البيانات
- [ ] البيانات تظهر على الصفحة

---

## 🆘 مشكلة؟

### ESP32 لا يتصل بـ WiFi
- ✅ تأكد من اسم الشبكة وكلمة المرور
- ✅ الشبكة يجب أن تكون 2.4GHz (ليس 5GHz)

### ESP32 لا يرسل للسيرفر
- ✅ تأكد من IP Address صحيح
- ✅ ESP32 والكمبيوتر على نفس الشبكة
- ✅ السيرفر يعمل (تحقق من Terminal)

### الصفحة تظهر "غير متصل"
- ✅ أعد تحميل الصفحة (F5)
- ✅ افتح Console (F12) وتحقق من الأخطاء

---

## 🎯 التالي؟

### جرّب هذه الميزات:
1. **📊 الرسم البياني** - شاهد تطور القراءات
2. **📥 التصدير** - احفظ البيانات كـ CSV
3. **📈 الإحصائيات** - أعلى وأدنى القيم
4. **🔄 التحديث** - البيانات تتحدث تلقائياً!

### خصّص التطبيق:
- غيّر الألوان في `styles.css`
- عدّل معدل الإرسال في ESP32
- أضف ميزات جديدة!

---

## 📖 المزيد من المعلومات

راجع [README.md](README.md) للتفاصيل الكاملة!

---

**🎉 مبروك! تطبيقك يعمل الآن!** 🌡️💧

